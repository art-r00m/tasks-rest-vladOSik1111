# Tasks CLI - Задание по программированию

## 📋 Описание проекта

Вам необходимо создать консольное приложение для управления задачами (TODO list) на языке Go. Приложение должно работать в интерактивном режиме и поддерживать базовые операции с задачами.

## 🎯 Требования к реализации

### Обязательные файлы

Ваш проект **обязательно** должен содержать следующие файлы:

```
project/
├── main.go           # Точка входа, обработка команд
├── task.go           # Структура Task и её методы
├── storage.go        # Интерфейс Storage
├── memory.go         # Реализация хранилища в памяти
├── json.go           # Реализация хранилища в JSON (опционально для высшей оценки)
└── main_test.go      # (мои тесты вашего кода, нельзя редачить)
```

### Структура Task

```go
type Task struct {
    ID        int
    Title     string
    Status    string    // "TODO" или "DONE"
    CreatedAt time.Time
}
```

### Интерфейс Storage

```go
type Storage interface {
  // ...
}
```

## 📝 Команды приложения

### Интерактивный режим

Приложение должно запускаться в интерактивном режиме и принимать команды через stdin:

```bash
./tasks --storage=memory
> add "Купить молоко"
Task added with ID: 1
> list
1. [TODO] Купить молоко (2025-10-23)
> done 1
Task 1 marked as done
> exit
Goodbye!
```

### Список команд

1. **add "название"** - добавить новую задачу

   ```
   > add "Купить молоко"
   Task added with ID: 1
   ```

2. **list** - показать все задачи

   ```
   > list
   1. [TODO] Купить молоко (2025-10-23)
   2. [DONE] Написать код (2025-10-22)
   ```

3. **list --status todo|done** - фильтрация по статусу

   ```
   > list --status todo
2. [TODO] Написать отчет (2025-10-23)
3. [TODO] Позвонить клиенту (2025-10-23)

> list --status done

1. [DONE] Купить продукты (2025-10-23)

> delete 3
Task 3 deleted
> list

1. [DONE] Купить продукты (2025-10-23)
2. [TODO] Написать отчет (2025-10-23)

> edit 2 "Отправить отчет директору"
Task 2 updated
> list

1. [DONE] Купить продукты (2025-10-23)
2. [TODO] Отправить отчет директору (2025-10-23)

> exit
Goodbye!

```

---

## 🚀 Начало работы

1. **Клонируйте репозиторий**
   ```bash
   git clone <your-repo-url>
   cd <your-repo>
   ```

2. **Создайте необходимые файлы**

   ```bash
   touch main.go task.go storage.go memory.go json.go main_test.go
   ```

3. **Инициализируйте Go модуль**

   ```bash
   go mod init tasks-cli
   ```

4. **Начните с базовой структуры**
   - Определите структуру `Task` в `task.go`
   - Создайте интерфейс `Storage` в `storage.go`
   - Реализуйте `MemoryStorage` в `memory.go`
   - Напишите основную логику в `main.go`

5. **Тестируйте локально**

   ```bash
   go build -o tasks
   ./tasks --storage=memory
   ```

6. **Коммитьте и пушьте**

   ```bash
   git add .
   git commit -m "Initial implementation"
   git push origin main
   ```

7. **Проверьте результаты в Actions**

Удачи! 🍀

   1. [TODO] Купить молоко (2025-10-23)

   ```

4. **done <id>** - отметить задачу выполненной
   ```

   > done 1
   Task 1 marked as done

   ```

5. **undone <id>** - вернуть задачу в статус TODO
   ```

   > undone 1
   Task 1 marked as todo

   ```

6. **delete <id>** - удалить задачу
   ```

   > delete 1
   Task 1 deleted

   ```

7. **edit <id> "новое название"** - изменить название (бонус)
   ```

   > edit 1 "Новое название"
   Task 1 updated

   ```

8. **exit** - выход из программы

## 🎓 Критерии оценки

### ⭐ Удовлетворительно (60 баллов)

- ✅ Проект компилируется без ошибок (5 баллов)
- ✅ Команда `add` работает корректно (10 баллов)
- ✅ Команда `list` выводит список задач (10 баллов)
- ✅ Команда `done` отмечает задачу выполненной (10 баллов)
- ✅ Команда `undone` возвращает задачу в TODO (10 баллов)
- ✅ Команда `delete` удаляет задачу (10 баллов)
- ✅ Обработка неправильных команд с выводом ошибки (5 баллов)

### ⭐⭐ Хорошо (90 баллов)

Все из "Удовлетворительно" +

- ✅ Фильтрация `list --status todo` (10 баллов)
- ✅ Фильтрация `list --status done` (10 баллов)
- ✅ Валидация пустого заголовка (5 баллов)
- ✅ Валидация длинного заголовка (>200 символов) (5 баллов)
- ✅ Обработка ошибок для несуществующих задач (5 баллов)

### ⭐⭐⭐ Отлично (100 баллов)

Все из "Хорошо" +

- ✅ JSON хранилище с персистентностью (10 баллов)
- ✅ Команда `edit` для изменения названия (5 баллов, опционально)
- ✅ Unit тесты в `main_test.go` (10 баллов)

## 🔧 Технические требования

### Валидация

- Заголовок не может быть пустым
- Максимальная длина заголовка - 200 символов
- ID должен быть валидным числом
- Ошибки выводятся с префиксом "Error:"

### Обработка ошибок

```go
// Примеры правильного вывода ошибок:
Error: title cannot be empty
Error: title is too long
Error: task not found
Error: invalid task ID
Error: unknown command 'invalid_command'
```

### Формат вывода

```
Task added with ID: 1
Task 1 marked as done
Task 1 marked as todo
Task 1 deleted
Task 1 updated

1. [TODO] Название задачи (2025-10-23)
2. [DONE] Другая задача (2025-10-22)
```

### Поддержка флагов

```bash
# Хранилище в памяти (по умолчанию)
./tasks --storage=memory

# JSON хранилище (для высшей оценки)
./tasks --storage=json
```

## 🧪 Тестирование

### Локальная проверка

```bash
# Сборка проекта
go build -o tasks

# Ручное тестирование
./tasks --storage=memory
> add "Тестовая задача"
> list
> exit

# Запуск unit тестов
go test -v

# Запуск с покрытием
go test -cover
```

### Автоматическая проверка

При каждом push в GitHub будут автоматически запускаться тесты. Результаты можно посмотреть:

1. Откройте вкладку **Actions** в вашем репозитории
2. Выберите последний workflow run
3. Изучите детали каждого теста
4. Посмотрите итоговый балл в разделе Summary

## 💡 Подсказки

### Парсинг команд

Для обработки команд с кавычками используйте простой парсер:

```go
func parseCommand(line string) []string {
    var parts []string
    var current strings.Builder
    // ...

    for i := 0; i < len(line); i++ {
        // ...
    }
    
    // ...
    
    return parts
}
```

### Интерактивный режим

```go
scanner := bufio.NewScanner(os.Stdin)

for {
    fmt.Print("> ")
    // ...
   
    handleCommand(storage, line)
}
```

## ❌ Частые ошибки

1. **Неправильный формат вывода** - тесты проверяют точные строки
2. **Отсутствие обработки ошибок** - все ошибки должны выводиться в stderr
3. **Забыли про фильтрацию** - `list --status` обязателен для хорошей оценки
4. **Неправильная структура файлов** - файлы должны называться точно как указано
5. **Нет валидации** - проверяйте пустые и длинные заголовки
6. **Не обрабатываете кавычки** - заголовки могут содержать пробелы

## 🆘 Поддержка

Если тесты не проходят:

1. Проверьте точность формата вывода
2. Убедитесь в правильной обработке ошибок
3. Запустите тесты локально: `go test -v`
4. Изучите логи в GitHub Actions

## 📊 Пример работы программы

```bash
$ ./tasks --storage=memory
> add "Купить продукты"
Task added with ID: 1
> add "Написать отчет"
Task added with ID: 2
> add "Позвонить клиенту"
Task added with ID: 3
> list
1. [TODO] Купить продукты (2025-10-23)
2. [TODO] Написать отчет (2025-10-23)
3. [TODO] Позвонить клиенту (2025-10-23)
> done 1
Task 1 marked as done
> list --status todo
